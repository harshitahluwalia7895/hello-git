1.git init   --> its created a .git folder ,this folder is where all the magic happens,it keeps tracks of all your files base on the commands that you use.
2.git status --> Git is simply telling us we made samone changes in our repository and those changes are not tracking right now .
3.git add filename
4.git commit -m"your msg"

